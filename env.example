# Configuration Supabase (OBLIGATOIRE)
# Obtenez ces valeurs depuis votre projet Supabase > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Configuration Stripe (optionnel pour les tests)
# Obtenez cette valeur depuis votre dashboard Stripe > Developers > API keys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Configuration de l'application (optionnel)
NEXT_PUBLIC_APP_NAME="Caisse 2"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Configuration SMTP pour l'envoi d'emails (optionnel)
# Si non configuré, utilise un serveur de test
SMTP_HOST=mail.startindev.com
SMTP_PORT=465
SMTP_USER=noreply@startindev.com
SMTP_PASS=startindev#23
SMTP_FROM=noreply@startindev.com

# Mode d'envoi d'email : 'production' (par défaut), 'simulate' (aucun email envoyé)
EMAIL_MODE=production
